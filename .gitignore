# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Runtime data
server/data/
server/uploads/

# Secrets / env — never commit real values; .env.example holds placeholders
.env
.env.*
!.env.example
*.pem

# Wrangler local state
.wrangler/

# Local-only Claude notes (names real infrastructure)
CLAUDE.local.md

# Private operator context for creator acquisition. This names production
# infrastructure and workflows that must not appear in the public repository.
/GROWTH_OPERATOR.local.md

# Internal legal drafts + risk assessments (this repo is public). The public
# legal pages live in src/legal/ and ARE committed; these are the working docs.
/legal/
.e2e-data/
test-results/
playwright-report/
blob-report/
.playwright/
